// creating test for userRouter
test('should exist', () => {
    expect(userRouter).toBeDefined();
}
);
//
// creating test for userRouter
test('should be a function', () => {
    expect(typeof userRouter).toBe('function');
}
);