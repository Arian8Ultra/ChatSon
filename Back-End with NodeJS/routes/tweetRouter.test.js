// test for tweetRouter

// creating test for tweetRouter
test('should exist', () => { 
    expect(tweetRouter).toBeDefined();
});

// creating test for tweetRouter
test('should be a function', () => {
    expect(typeof tweetRouter).toBe('function');
});
